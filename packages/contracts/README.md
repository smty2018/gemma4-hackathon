# Shared contracts

Language-neutral JSON schemas shared between the web client, API, workers, and evaluation tooling live here. Generate language-specific types from these files rather than maintaining duplicate handwritten contracts.
